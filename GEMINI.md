# 開発ルール (gdrive-receipts-to-mf-csv)

本プロジェクトにおける追加の開発規約です。

## 1. テストコードの保守と実行
- コード（`code.js`）を修正した後は、**必ず対応するテストコード（`code.test.js`）も最新化**してください。
- 修正完了後は、ローカル環境で **`npm run test`（または `npm test`）を実行し、すべてのテストがパスすることを確認**してください。
- テストが通らない状態でのコミットやプッシュ、プルリクエスト作成は行わないでください。
